module Merge1;

// This example should be fine wrt. merging
// But not supported by Scala code gen. due duplicate message label occurrence
// So all the use cases of mergeability wrt. merging duplicate branch cases aren't supported by this implementation
// It should be one of the examples supported by merging, but not supported by vanilla Scribble
global protocol Proto(role A, role B, role C) {
  choice at A  {
    1a() from A to B;
    M() from B to C;
    1b() from B to C;
  } or {
    2a() from A to B;
    M() from B to C;
    1c() from B to C;
  }
}
